    >>> from common import *

    >>> from dnews.smodel.saramin	import SaraminIt, SaraminItTest
    >>> from dnews.scraper		import Scraper
    >>> from dnews.model_tools		import NetTools
    >>> from lxml			import etree

* error1

** prepare

    >>> url = "http://www.saramin.co.kr/zf_user/upjikjong-recruit/upjikjong-list/pageCount/500/categoryCode/9%7C4/category_level/top/page/1/order/RD"
    >>> sample_html = '__tmp/saramin_error_sample2.html'

    >>> NetTools.save_page(url, sample_html) #doctest: +SKIP
    >>> data = NetTools.read(sample_html)

** debug

    >>> model = SaraminIt()
    >>> infos = model.get(data)

T: The problem is that. We couldn't parse the city

    >>> for key in infos.keys():
    ...     print(len(infos[key]), key) #doctest: +SKIP
    500 date_late
    500 date
    500 corpname
    500 money
    500 corpidx
    500 content
    500 count
    500 submit_num
    500 spam
    500 idx
    500 corp_employ_num
    500 title
    500 priority
    500 body
    0 city

    >>> len(infos['city']) #doctest: +SKIP
    500

** choose selector for city

    >>> tree = etree.HTML(data)
    >>> xpath = '//div[@class="corp-con hs_corp"]//li//span[1]'
    >>> sel_list = tree.xpath(xpath)
    >>> len(sel_list)
    500
    >>> sel_list[0].text = u('전주시 덕진구')


The site uses ajax to get the info of the company. The request is

http://www.saramin.co.kr/zf_user/jobs/job/summary?rec_idx=17858153&t_category=jobcategory_recruit&t_content=generic

